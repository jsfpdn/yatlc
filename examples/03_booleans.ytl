
i32 main() {
    bool a = true;
    bool b = false;

    bool c = a && b && b && a || a;

    bool d = a || b || b || a && c;

    bool e = (c and b) or (b and c);

    bool f = 10 < 12;

    bool h = not h;

    h &= h;
    h |= h;

    return 0;
}
