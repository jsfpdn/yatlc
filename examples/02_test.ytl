i64 main(i32 a, i32 b) {
    i32 c = 0;
    c = 10;

    while (true) {
        c += b;

        break;
    }

    for (i32 i = 0, i < 10, i++) {
        i32 a = 10;
    }

    i32 d = c == 14 ? a : 30;

    if (c < 25) {
        c += 10;
    } else {
        c += 20;
    }

    if (c > 30) {
        c -= 10;
    }

    return @i64(a + b);
}
